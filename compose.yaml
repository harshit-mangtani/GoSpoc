services:
  postgres:
     image: postgres:17
     container_name: gospoc-postgres
     environment:
        POSTGRES_DB: judge
        POSTGRES_USER: judge
        POSTGRES_PASSWORD: judge
     ports:
        - "5432:5432"
     volumes:
        - postgres_data:/var/lib/postgresql/data 

volumes:
  postgres_data: